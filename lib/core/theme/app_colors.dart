import 'package:flutter/material.dart';

/// Balam.AI brand colors — warm, trustworthy, nurturing
class AppColors {
  AppColors._();

  // Primary — Warm coral/rose (nurturing, feminine but not exclusive)
  static const primary = Color(0xFFE8787A);
  static const primaryLight = Color(0xFFF4A9AB);
  static const primaryDark = Color(0xFFCC5C5E);

  // Secondary — Soft teal (trust, calm, medical credibility)
  static const secondary = Color(0xFF5BBCB4);
  static const secondaryLight = Color(0xFF8DD4CE);
  static const secondaryDark = Color(0xFF3A9A92);

  // Accent — Warm gold (premium, milestone celebrations)
  static const accent = Color(0xFFF5C15A);
  static const accentLight = Color(0xFFF8D68A);
  static const accentDark = Color(0xFFD4A23B);

  // Neutrals
  static const background = Color(0xFFFAF8F6);
  static const surface = Color(0xFFFFFFFF);
  static const cardBg = Color(0xFFFFF5F5);
  static const textPrimary = Color(0xFF2D2D3A);
  static const textSecondary = Color(0xFF6E6E82);
  static const textHint = Color(0xFFB0B0C0);
  static const divider = Color(0xFFEEEEF2);
  static const error = Color(0xFFE74C3C);
  static const success = Color(0xFF27AE60);
  static const warning = Color(0xFFF39C12);

  // Journey stage colors
  static const stagePrePregnancy = Color(0xFFB39DDB);
  static const stagePregnancy = Color(0xFFE8787A);
  static const stageNewborn = Color(0xFF5BBCB4);
  static const stageToddler = Color(0xFFF5C15A);
}
